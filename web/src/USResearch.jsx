import React,{useEffect,useState} from 'react';
export default function USResearch(){
 const [report,setReport]=useState(null);
 useEffect(()=>{let active=true;fetch('/api/us/replay').then(r=>r.ok?r.json():null).then(r=>{if(active)setReport(r)}).catch(()=>{});return()=>{active=false}},[]);
 if(!report)return null;
 return <section className="panel detail-panel"><h2>US strategy screening</h2><p className="notice">Historical display-price scenarios, not verified fills. Open basis is reported separately. No profitable US strategy has been established.</p><p>{report.coverage.length} sampled markets · {report.coverage.reduce((n,r)=>n+(r.points||0),0)} usable observations · {report.coverage.filter(r=>r.verified_settlement!==null&&r.verified_settlement!==undefined).length} verified settlement values</p><img src="/api/us/replay/chart" alt="US momentum and mean-reversion realized profit and loss, with spread, fees and slippage scenarios; unresolved open positions shown separately" style={{width:'100%',height:'auto'}}/><div className="table-scroll"><table><thead><tr><th>Strategy</th><th>Slippage / side</th><th>Entries / exits</th><th>Realized P&amp;L</th><th>Open basis</th></tr></thead><tbody>{report.results.map(r=><tr key={r.strategy+r.slippage_per_side}><td>{r.strategy.replaceAll('_',' ')}</td><td>${Number(r.slippage_per_side).toFixed(3)}</td><td>{r.entries} / {r.exits}</td><td>${Number(r.realized_pnl).toFixed(2)}</td><td>${Number(r.open_basis).toFixed(2)}</td></tr>)}</tbody></table></div><p>No paid subscription is configured. Trading fees and selected slippage are included. Archived graphs may include a separately labeled, hypothetical $200/month sensitivity; that is not an actual expense. Each scenario has its own $50 account and $40 cash reserve.</p><details><summary>Method and limitations</summary><ul>{report.limitations.map(t=><li key={t}>{t}</li>)}</ul></details></section>
}
