import React,{useEffect,useState} from 'react';
const money=value=>Number(value).toLocaleString('en-US',{style:'currency',currency:'USD'});
export default function USBasketStudy(){
 const [report,setReport]=useState(null);
 useEffect(()=>{let active=true;fetch('/api/us/baskets').then(r=>r.ok?r.json():null).then(r=>{if(active)setReport(r)}).catch(()=>{});return()=>{active=false}},[]);
 if(!report)return null;
 return <section className="panel detail-panel"><h2>US price inefficiency test</h2><p>Complete weather outcome baskets: test whether combined prices plus fees leave a positive settlement payout margin. Every leg must refresh after the decision, with at most ten seconds between leg timestamps.</p><p className="notice">These are historical price scenarios. Partial fills can lose money, and quote history does not prove available depth or completed fills. No live arbitrage profit is claimed.</p><div className="table-scroll"><table><thead><tr><th>Basket</th><th>Slip/side</th><th>Initial signals</th><th>Completed scenarios</th><th>Trading P&amp;L</th></tr></thead><tbody>{report.results.map(r=><tr key={r.strategy+r.slippage_per_side}><td>{r.strategy.replaceAll('_',' ')}</td><td>{Number(r.slippage_per_side).toFixed(3)} USD</td><td>{r.initial_inefficiency_signals}</td><td>{r.completed_basket_scenarios}</td><td>{money(r.realized_pnl)}</td></tr>)}</tbody></table></div><details><summary>Archived graph with hypothetical $200/month sensitivity (not a real expense)</summary><a href="/api/us/baskets/chart" target="_blank" rel="noreferrer"><img src="/api/us/baskets/chart" alt="US complete-ladder inefficiency test profit graph; open full size" style={{width:'100%',height:'auto'}}/></a></details><details><summary>Basket coverage and partial-fill risk</summary><p>{report.coverage.length} registered dates · {report.coverage.reduce((n,r)=>n+r.price_records,0)} usable price samples. Each reported basket includes its worst loss across incomplete subsets of filled legs in the source report.</p><ul>{report.limitations.map(t=><li key={t}>{t}</li>)}</ul></details></section>
}
